# This configuration file was generated by `ameba --gen-config`
# on 2023-11-14 08:54:37 UTC using Ameba version 1.5.0.
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.

# Problems found: 10
# Run `ameba --only Naming/Filename` for details
Naming/Filename:
  Description: Enforces file names to be in underscored case
  Excluded:
    - examples/HelloWorld_publisher.cr
    - examples/PublishSubscribe_consumer.cr
    - examples/PublishSubscribe_publisher.cr
    - examples/Routing_consumer.cr
    - examples/Routing_publisher.cr
    - examples/Topics_consumer.cr
    - examples/Topics_publisher.cr
    - examples/WorkQueues_consumer.cr
    - examples/WorkQueues_publisher.cr
    - examples/HelloWorld_consumer.cr
  Enabled: true
  Severity: Convention

# Problems found: 11
# Run `ameba --only Lint/NotNil` for details
Lint/NotNil:
  Description: Identifies usage of `not_nil!` calls
  Excluded:
    - spec/websocket_spec.cr
    - spec/amqp-client_spec.cr
    - src/amqp-client/channel.cr
  Enabled: true
  Severity: Warning

Naming/BlockParameterName:
  MinNameLength: 1
  AllowNamesEndingInNumbers: true
  Enabled: true
  Severity: Convention
